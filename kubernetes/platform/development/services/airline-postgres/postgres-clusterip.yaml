apiVersion: v1
kind: Service
metadata:
  name: airline-postgres
  labels:
    db: airline-postgres
spec:
  type: ClusterIP
  selector:
    db: airline-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432