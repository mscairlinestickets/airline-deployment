apiVersion: apps/v1
kind: Deployment
metadata:
  name: airline-redis
  labels:
    db: airline-redis
spec:
  selector:
    matchLabels:
      db: airline-redis
  template:
    metadata:
      labels:
        db: airline-redis
    spec:
      containers:
        - name: airline-redis
          image: redis:7.2
          resources:
            requests:
              cpu: 100m
              memory: 50Mi
            limits:
              cpu: 200m
              memory: 100Mi

---

apiVersion: v1
kind: Service
metadata:
  name: airline-redis
  labels:
    db: airline-redis
spec:
  type: ClusterIP
  selector:
    db: airline-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379